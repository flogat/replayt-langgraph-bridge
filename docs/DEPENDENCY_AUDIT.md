# Dependency Audit Policy

## Overview

This project uses `pip-audit` to scan for known vulnerabilities in dependencies. The CI workflow runs this audit on every push and pull request.

## Audit command

